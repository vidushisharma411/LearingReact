*header 
 *logo
 *nav item

 *Body
 *search
 *restaurant container
 *restaurant card
*footer
 *copyright
 *links
 *address
 *contect 

 // React.createElement hme object deta hai fir or fir jb hum ise render krenge to ReactDOM iss object ko render krke browser me push kr deti hai

// jsx(before it reaches the js ) --> parcel --> bable

// jsx ==> React.createElement ==> js element(object) ==> html element (when rendered)
/*
// const heading = React.createElement("h1", {id: "heading", xyz: "hdhhjgj"}, "Hellow world");
// const root = ReactDOM.createRoot(document.getElementById("root"));

// root.render(heading);
// console.log(heading);

two types of import/export

default  export/import  
export default compunent ;
import component from "path";


Named export/import 

export const component 
import {component} from "path";  